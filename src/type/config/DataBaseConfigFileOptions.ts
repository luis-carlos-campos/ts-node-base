/**
 * Type definition for each entry of LoggerConfigFile.
 */
type LoggerConfig = {
    host: string;
    port: number;
    username: string;
    password: string;
    database: string;
};

export default LoggerConfig;
